<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ViewModels="clr-namespace:Rodkulman.MilkMafia.ViewModels"
             mc:Ignorable="d"
             x:Class="Rodkulman.MilkMafia.Views.LoginView">
    <ContentPage.BindingContext>
        <ViewModels:LoginViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <Grid>
            <StackLayout>
                <Entry IsPassword="True" Placeholder="CPF" Text="{Binding CPF}" />
                <Button Text="Login" Padding="20,5" Command="{Binding LoginCommand}" />
            </StackLayout>
            <Grid IsVisible="{Binding IsBusy}" BackgroundColor="#88000000">
                <ActivityIndicator IsRunning="{Binding IsBusy}" VerticalOptions="Center" HorizontalOptions="Center" />
            </Grid>
        </Grid>
    </ContentPage.Content>
</ContentPage>